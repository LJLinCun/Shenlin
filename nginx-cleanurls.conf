# 在 server 块或 location / 中添加：
location / {
    # 按顺序尝试：原路径 → 加 .html → 目录 → 404
    try_files $uri $uri.html $uri/ =404;
}

# 可选：将 .html 结尾的请求 301 重定向到无后缀 URL（对 SEO 友好）
location ~ \.html$ {
    rewrite ^(.+)\.html$ $1 permanent;
}
